# OpenMesh V2 需求说明文档

> **文档性质**：产品需求说明文档（PRD）
> **文档作者**：项目负责人
> **创建日期**：2026-03-27
> **文档版本**：v1.2
> **阅读对象**：架构师、工程师、测试团队

---

## 一、产品定位

OpenMesh V2 是流量行业的 OpenRouter。

OpenRouter 让用户能够统一接入不同 AI 模型供应商，OpenMesh 让用户能够统一接入不同 VPN 流量供应商。两者的核心逻辑一致：**平台负责统一接入和结算协议，内容（流量）由供应商提供，用户自由选择**。

对应关系如下：

| OpenRouter | OpenMesh V2 |
|-----------|-------------|
| AI 模型供应商 | 流量供应商 |
| 模型套餐（价格/月度 Token 额度） | 订阅套餐（价格/月度流量额度） |
| API Key | x402 订阅凭证 |
| 统一 API 接入 | 统一 VPN 接入（sing-box） |
| 按模型订阅计费 | 按供应商订阅计费 |

**计费模式**：月度订阅制。用户通过 x402 协议将固定月费（USDC）支付到平台为供应商创建的智能合约，到期自动续订。供应商提现时平台收取 10% 手续费。

---

## 二、平台角色定义

### 2.1 三类角色

| 角色 | 描述 |
|------|------|
| 用户 | 购买订阅、使用 VPN 服务的终端用户 |
| 流量供应商 | 提供 VPN 节点和流量、自定义订阅套餐、独立管理用户订阅的服务提供方 |
| OpenMesh 平台 | 提供统一接入协议（x402 订阅）、流量转发规范、供应商发现机制、智能合约托管结算，以及官方参考实现 |

### 2.2 平台职责边界

OpenMesh 平台的核心职责是：

- **流量转发**：提供统一的 VPN 数据通道规范（基于 Hysteria2），确保任何经过认证的用户流量都能正确转发
- **x402 订阅协议**：提供标准化的订阅支付和验证规范，供应商必须遵守这个协议
- **供应商发现**：维护去中心化的供应商注册表（链上），让用户能够发现并选择供应商
- **智能合约托管结算**：为每个供应商创建和管理订阅收款智能合约，用户订阅费通过 x402 协议转入平台指定的智能合约，供应商提现时平台收取 10% 手续费
- **供应商准入管理**：供应商上线服务需缴纳 100 USDC 上线费，平台为其创建专属智能合约
- **官方参考实现**：提供一套完整的、开源的流量管理和套餐管理系统，供无自建能力的供应商直接使用

**平台的结算模式**：
- 平台为每个供应商创建独立的智能合约用于收款
- 用户通过 x402 协议将订阅费支付到平台指定的智能合约
- 供应商可随时从智能合约提现，平台自动扣除 10% 手续费
- 供应商上线时需一次性支付 100 USDC 上线费

**平台不做的事**：
- 不强制供应商使用官方管理系统——只要遵守 x402 订阅协议和流量转发规范，供应商可以使用自己的后端系统

---

## 三、供应商接入模式

### 3.1 两种接入模式

**模式 A：使用官方系统（推荐新供应商）**

供应商没有自建系统，直接部署 OpenMesh 官方开源的流量管理和套餐管理系统。供应商只需要配置自己的节点和套餐，其余功能开箱即用。

**模式 B：使用自有系统**

供应商已有成熟的流量管理/用户管理系统，不需要使用官方实现。只要其系统实现了 x402 订阅协议接口和标准的流量转发规范，即可接入 OpenMesh 平台被用户发现和使用。

### 3.2 官方系统（本期需要实现）

官方系统是 OpenMesh 平台本期交付的核心服务端工程，包含两个子系统：

**流量管理系统**

负责识别每个用户的流量，并根据其订阅等级执行对应的服务策略（月度流量额度监控与 Fair Use 执行）。

**套餐管理系统**

负责供应商定义和管理订阅套餐，处理用户的订阅购买、续订、状态查询和取消。

两个子系统均以开源形式发布，供应商可以自行部署和定制，或直接使用 OpenMesh 托管版本。

---

## 四、订阅套餐设计

### 4.1 套餐由供应商定义和控制

每个流量供应商完全控制自己提供的订阅套餐，包括等级划分、价格设定、各等级的服务参数。OpenMesh 平台不参与套餐内容的定义，只规范套餐的格式和发布方式。

### 4.2 套餐等级（Tier）

订阅套餐按等级划分，不同等级在以下维度上有明确区别：

| 维度 | 说明 |
|------|------|
| **月费价格** | 以 USDC 计，每 30 天收取一次 |
| **月度流量额度（Fair Use）** | 当月流量参考上限，类似大模型的月度 Token 额度；超出后供应商可选择降速，不得直接断线 |
| **节点覆盖范围** | 该等级可使用哪些地区的节点 |
| **套餐名称** | 用户可识别的名称，如"基础版"、"标准版"、"旗舰版" |

**示例（供应商自行定义，以下仅为示意）**：

| 等级 | 月费 | 月度流量额度 |
|------|------|-------------|
| 基础版 | 3 USDC | 100 GB |
| 标准版 | 6 USDC | 300 GB |
| 旗舰版 | 12 USDC | 1 TB |

**说明**：
- 用户可在任意数量的设备上使用，不限制设备数
- 月度流量额度为 Fair Use 参考值，超出后供应商可降速，不得直接断开用户连接
- 不区分上行和下行，按总流量计算
- 订阅续订时，流量使用量自动重置为 0

（以上数字均为示例，实际由各供应商自主设定）

### 4.3 套餐管理规则

- 供应商可以定义任意数量的等级套餐，但至少需要上架一个有效套餐，才会在用户端展示
- 套餐可以随时上架或下架，下架不影响已订阅该套餐用户的当前周期服务
- 同一供应商的套餐等级，名称必须对用户清晰可辨，不允许模糊命名
- 套餐价格一旦有用户订阅，在该用户当前订阅周期内不得变更

---

## 五、功能需求

### 5.1 用户订阅购买

用户通过客户端浏览供应商和套餐，完成订阅购买，流程不超过 3 步。

**用户操作流程**

1. 浏览供应商列表，查看各供应商的套餐等级、月费、月度流量额度（Fair Use）、节点覆盖
2. 选择供应商和具体套餐等级，查看费用明细
3. 解锁钱包，通过 x402 协议完成支付
4. 订阅立即生效，VPN 服务立即可用，可在任意数量的设备上使用

**业务规则**

- 首次购买需用户主动解锁钱包并签名，系统不得自动发起首次扣费
- 支付成功后，VPN 服务必须在 1 分钟内可用
- 同一用户向同一供应商，在同一有效周期内不允许重复购买同一等级套餐
- 用户可以向多个不同供应商分别订阅，各订阅相互独立
- 不限制每个订阅的使用设备数量

### 5.2 订阅等级与流量管理（Fair Use）

**月度流量额度说明**

- 用户可在任意数量的设备上使用 VPN，不限制设备数
- 月度流量额度为 Fair Use 参考值，按当月上行+下行流量总和计算
- 流量达到套餐额度 80% 时，客户端发出提醒通知
- 流量超出套餐额度后，供应商可选择降速或继续提供服务，**不得直接断开用户连接**
- 订阅续订时，流量使用量自动重置为 0

**用量展示**

- 客户端提供用量展示界面，清晰呈现：
  - 套餐月度流量额度
  - 当月已使用流量
  - 剩余流量及使用百分比
  - 本周期流量重置日期
- 界面设计参考大模型（如 OpenRouter、Claude）的用量余额显示风格
- 数据延迟不超过 1 分钟

### 5.3 订阅生命周期

系统需要准确维护每个订阅的状态，并依据状态决定是否允许用户建立 VPN 连接。

**订阅状态及其含义**

| 状态 | 含义 | 用户是否可以使用 VPN |
|------|------|---------------------|
| 有效（Active） | 订阅在有效期内 | 可以 |
| 宽限期（Grace） | 到期后 24 小时内 | 可以 |
| 已过期（Expired） | 超出宽限期、续订未成功 | 不可以，连接被拒绝 |
| 已取消（Canceled） | 用户主动取消，等当前周期结束 | 当期内可以 |

**业务规则**

- 订阅有效期为 30 天，从支付成功时刻起算
- 有效期结束后进入 24 小时宽限期，宽限期内服务继续
- 宽限期结束后仍未续订，系统立即拒绝该用户的 VPN 连接请求
- 宽限期内续订成功，新周期从原到期时间起算，不惩罚延迟续订的用户
- 用户取消订阅后，当前付费周期内服务不中断，到期时自然停止

### 5.4 自动续订

**触发时机**

- 订阅到期前 3 天，系统开始尝试自动续订
- 首次尝试失败后，宽限期内每 6 小时自动重试一次，直到成功或宽限期结束

**执行方式**

- 自动续订由客户端发起，利用用户已授权的 Signer Session 构建续订支付并提交
- 服务端验证签名并更新订阅状态，不持有用户私钥，不从服务端主动发起扣款
- 续订成功后套餐等级保持不变（与上一周期相同）；用户若希望续订时升级，需手动操作

**用户对自动续订的控制权**

- 用户可以随时在客户端开启或关闭自动续订
- 用户可以设置自动续订的月度总金额上限
- Signer Session 的自动签名仅限订阅续订场景，单次金额不超过套餐当期价格

**续订失败处理**

- 续订失败后立即推送通知，明确告知失败原因（余额不足 / 需要重新授权）
- 进入宽限期，客户端持续显示"请尽快续订"提示
- 宽限期结束仍未续订，服务暂停

### 5.5 套餐升级与降级

用户可以在订阅期内更换同一供应商的不同等级套餐。

- **升级（换更高等级）**：需补交当前周期剩余天数的差价，立即生效，流量额度立即更新
- **降级（换更低等级）**：当前周期维持原等级服务，下个续订周期按新等级计费
- 升级/降级不重置订阅有效期

### 5.6 订阅管理

**用户可以查看的信息**

- 所有活跃订阅的供应商名称、套餐名称、套餐等级、到期时间、剩余天数
- 用量信息（参考大模型用量显示风格）：
  - 套餐月度流量额度
  - 当月已使用流量
  - 剩余流量及使用百分比
  - 本周期流量重置日期
- 自动续订开启状态
- 历史支付记录（时间、金额、套餐等级、覆盖周期）

**用户可以执行的操作**

- 开启或关闭自动续订
- 手动发起续订
- 升级套餐等级（立即生效）
- 取消订阅

### 5.7 服务暂停与恢复

**暂停**

- 宽限期截止后，该用户的 VPN 连接被断开，新的连接请求被拒绝
- 客户端明确提示"订阅已过期，请续订后使用"，不允许显示模糊的网络错误

**恢复**

- 续订成功后，VPN 服务在 1 分钟内自动恢复，用户无需手动操作

### 5.8 流量监控（Fair Use）

**用户侧**

- 客户端实时展示当月已使用流量（延迟不超过 1 分钟）
- 流量达到套餐额度 80% 时，客户端推送提醒通知
- 流量超出套餐额度后，客户端给出提示，说明当前供应商的 Fair Use 策略（降速或继续）

**供应商侧**

- 供应商可通过管理界面查看各订阅等级、各用户的流量使用概况
- 供应商可以为每个套餐等级分别设置月度流量额度和超额处理策略

**重要约束**

- 流量超额不得直接断开用户连接，服务中断的唯一条件是订阅过期
- 流量统计需满足 Fair Use 管理的基本要求

### 5.9 供应商准入与智能合约管理

**供应商上线流程**

1. 供应商提交上线申请，支付 100 USDC 上线费
2. 平台为供应商创建专属智能合约用于收款
3. 供应商获得智能合约地址，可以开始配置套餐和节点
4. 供应商在链上完成注册，用户端即可发现该供应商

**智能合约托管结算**

- 用户订阅时，通过 x402 协议将订阅费支付到平台为供应商创建的智能合约
- 智能合约自动记录每笔订阅收入
- 供应商可随时查看智能合约余额和收入明细

**供应商提现**

- 供应商可随时发起提现请求
- 平台自动从提现金额中扣除 10% 手续费
- 剩余 90% 转入供应商指定的钱包地址
- 提现记录可追溯，包括提现时间、金额、手续费

**业务规则**

- 上线费 100 USDC 为一次性费用，不可退还
- 智能合约由平台创建和管理，供应商不可直接操作合约
- 手续费比例固定为 10%，在提现时自动扣除
- 供应商可以查看但不能修改智能合约代码
- 提现无最低金额限制，但需支付链上 Gas 费用

---

## 六、用户场景

### 场景 1：新用户首次订阅

张三第一次使用 OpenMesh。

1. 打开客户端，创建或导入钱包
2. 进入供应商列表，看到多家供应商，每家都展示了不同等级套餐的月费、月度流量额度（Fair Use）、节点覆盖
3. 他选择了一家供应商的"标准版"套餐（6 USDC/月，300 GB Fair Use 额度）
4. 查看费用明细后确认，Face ID 解锁钱包，支付完成
5. 客户端提示"订阅成功，有效至 2026-04-26，月度流量额度 300 GB"
6. 立即可以使用 VPN，可在任意设备上连接

---

### 场景 2：流量使用达到 80%，收到提醒

李四订阅了"基础版"（100 GB Fair Use 额度/月）。

1. 他在多台设备上使用 VPN，本月已使用 82 GB
2. 客户端推送通知："本月流量已使用 82%（82/100 GB），请注意"
3. 他继续使用，流量超出 100 GB 后，供应商对其连接进行降速处理
4. 客户端提示："本月流量已超出套餐额度，当前处于降速状态，可升级套餐恢复正常速度"
5. 他选择升级到"标准版"（300 GB），补交当前周期剩余天数的差价，速度立即恢复

---

### 场景 3：到期前自动续订成功

王五的订阅还有 3 天到期，钱包余额充足，Signer Session 有效。

1. 系统在后台静默发起续订支付，成功
2. 客户端推送通知："订阅已自动续订，服务有效至 2026-05-26，套餐等级不变，流量已重置"
3. 王五全程无需操作，服务无中断

---

### 场景 4：自动续订失败，宽限期内手动补救

钱七钱包余额不足。

1. 到期前 3 天，自动续订失败
2. 客户端推送通知："续订失败，原因：钱包余额不足。服务宽限至 2026-04-29，请尽快续订"
3. 钱七充值后，在客户端点击"立即续订"，支付成功
4. 新周期从原到期时间 2026-04-26 起算，宽限期内服务未曾中断，流量使用量重置为 0

---

### 场景 5：宽限期结束，服务暂停后恢复

孙八宽限期结束前未续订。

1. 宽限期截止后，VPN 连接断开
2. 客户端显示"您的订阅已过期，请续订后继续使用"
3. 孙八完成续订，不超过 1 分钟，VPN 自动恢复，流量使用量重置为 0

---

### 场景 6：切换供应商（官方系统 vs 自有系统供应商）

周九当前使用的是一家自建了系统的成熟供应商，现在想试试一家规模较小、使用官方系统的新供应商。

1. 在供应商列表中，两种供应商显示方式一致，用户无感知供应商后端用的是哪套系统
2. 周九选择新供应商的套餐，完成订阅购买
3. 客户端切换到新供应商节点，连接秒级恢复
4. 客户端提示"你还有一个旧供应商的订阅在有效期内，是否取消？"，周九自主决定保留或取消

---

### 场景 7：供应商使用官方系统上架套餐

一家新的流量供应商想接入 OpenMesh 平台，没有自建技术系统。

1. 供应商支付 100 USDC 上线费，平台为其创建专属智能合约
2. 供应商部署 OpenMesh 官方开源的流量管理系统和套餐管理系统
3. 在套餐管理系统中定义三个等级套餐（基础/标准/旗舰），设定各等级的价格、月度流量额度
4. 在链上完成供应商注册
5. 用户端即可发现该供应商并订阅其套餐，支付通过 x402 协议转入平台为该供应商创建的智能合约

---

### 场景 8：供应商使用自有系统接入

一家已有成熟用户系统的供应商希望接入 OpenMesh 平台获取更多用户。

1. 供应商支付 100 USDC 上线费，平台为其创建专属智能合约
2. 该供应商在自有系统中实现 OpenMesh 定义的 x402 订阅协议接口
3. 按照 OpenMesh 流量转发规范配置其 Hysteria2 节点
4. 在链上完成供应商注册，提交套餐信息
5. 用户端即可发现并订阅，该供应商的实际订阅管理、用户管理由其自有系统处理，平台负责流量转发、智能合约托管和 x402 支付验证

---

### 场景 9：供应商提现收入

供应商 A 已经运营了一个月，智能合约中累积了用户订阅收入。

1. 供应商登录管理后台，查看智能合约余额为 500 USDC
2. 供应商发起提现请求，输入提现金额 500 USDC
3. 系统显示：提现金额 500 USDC，手续费 50 USDC（10%），实际到账 450 USDC
4. 供应商确认后，450 USDC 转入供应商钱包，50 USDC 作为平台手续费
5. 提现记录保存，供应商可随时查看历史提现明细

---

## 七、优先级

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 供应商智能合约创建与管理 | P0 | 平台核心收入来源，托管结算基础 |
| 供应商上线费收取（100 USDC） | P0 | 供应商准入机制 |
| 用户订阅支付到智能合约 | P0 | 核心收费路径 |
| 供应商提现与手续费扣除（10%） | P0 | 平台收入实现 |
| 官方流量管理系统（开源） | P0 | 本期核心交付之一 |
| 官方套餐管理系统（开源） | P0 | 本期核心交付之一 |
| x402 订阅协议规范 | P0 | 供应商接入的基础协议 |
| 供应商定义多等级订阅套餐 | P0 | 没有套餐用户无法订阅 |
| 用户首次订阅购买 | P0 | 核心收费路径 |
| 月度流量额度（Fair Use）统计与展示 | P0 | 用户透明度，等级区分的参考维度 |
| 订阅生命周期管理 | P0 | 决定用户能否使用服务 |
| 服务准入校验（按订阅状态决定是否允许连接） | P0 | 计费闭环，服务中断唯一条件是订阅过期 |
| 自动续订与流量重置 | P0 | 订阅制体验核心 |
| 续订失败通知与宽限期 | P0 | 防止用户无感知断线 |
| 流量使用提醒（达到 80% 时通知） | P0 | 用户知情权 |
| Fair Use 超额后供应商降速策略执行 | P0 | 超额处理，不断线 |
| 智能合约余额查询与提现记录 | P1 | 供应商财务透明度 |
| 用户查看订阅信息和历史账单 | P1 | 基础管理能力 |
| 手动取消订阅 | P1 | 用户对订阅的控制权 |
| 套餐升级（立即生效） | P1 | 用户提升等级的路径 |
| 套餐降级（下期生效） | P2 | 首期可不做 |
| 季度/年度订阅 | P2 | 首期不做 |
| 试用期 | P3 | 待评估 |

---

## 八、验收标准

### 8.1 供应商准入与智能合约

- [ ] 供应商支付 100 USDC 上线费后，平台自动为其创建专属智能合约
- [ ] 智能合约地址唯一且不可变更，与供应商账户绑定
- [ ] 供应商可以查看智能合约地址和余额
- [ ] 用户订阅支付正确转入对应供应商的智能合约
- [ ] 供应商提现时，系统自动扣除 10% 手续费，剩余 90% 转入供应商钱包
- [ ] 提现记录完整可追溯，包括时间、金额、手续费明细

### 8.2 官方系统

- [ ] 官方流量管理系统和套餐管理系统以开源形式交付，附部署文档
- [ ] 供应商可以独立部署官方系统，无需依赖 OpenMesh 平台托管
- [ ] 已有自建系统的供应商，只要实现 x402 协议接口，即可接入 OpenMesh 平台
- [ ] 用户侧无法感知一个供应商使用的是官方系统还是自建系统，体验一致

### 8.2 套餐与等级

- [ ] 供应商可以定义至少 3 个不同等级的订阅套餐
- [ ] 每个套餐等级可以独立设置价格和月度流量额度
- [ ] 客户端正确展示各套餐等级的差异，用户可以清晰对比
- [ ] 套餐下架后，已订阅该套餐的用户当期服务不受影响

### 8.3 月度流量（Fair Use）

- [ ] 客户端正确展示当月已使用流量和套餐额度，延迟不超过 1 分钟
- [ ] 流量达到套餐额度 80% 时，客户端推送提醒通知
- [ ] 流量超出套餐额度后，供应商可执行降速策略，但不得直接断开用户连接
- [ ] 超额后客户端明确告知用户当前降速状态，并提示可升级套餐
- [ ] 订阅续订时，流量使用量自动重置为 0

### 8.4 订阅购买与续订

- [ ] 支付成功后，VPN 服务在 1 分钟内可用
- [ ] 订阅到期前 3 天内，钱包余额充足且授权有效时，系统自动完成续订，服务无中断
- [ ] 自动续订失败时，用户立即收到通知，说明明确的失败原因
- [ ] 宽限期内续订成功，新周期从原到期时间起算

### 8.5 服务暂停与恢复

- [ ] 宽限期结束后，VPN 连接断开，客户端显示"订阅已过期"，不显示模糊的网络错误
- [ ] 续订成功后，不超过 1 分钟 VPN 服务自动恢复
- [ ] 服务中断的唯一条件是订阅过期，流量超额不触发断线

---

## 九、待确认的开放问题

| 编号 | 问题 | 需要谁决策 | 状态 |
|------|------|-----------|------|
| Q1 | 官方系统是 OpenMesh 负责托管运维，还是仅提供开源代码由供应商自行部署？还是两者都支持？ | 产品 | 已确认：仅提供开源代码，供应商自行部署 |
| Q2 | x402 订阅协议接口规范由谁定义、在哪里发布、如何版本管理？ | 架构 | 未确认 |
| Q3 | 套餐升级时差价的计算方式：按剩余天数折算还是按其他规则？ | 产品 | 已确认：按剩余天数折算差价 |
| Q4 | 一个用户账号最多可以同时向几个不同供应商订阅？是否有上限？ | 产品 | 已确认：不限制 |
| Q5 | Fair Use 超限后降速策略由供应商自行决定，是否要求供应商在套餐页面提前明示降速幅度？ | 产品 | 未确认 |
| Q6 | 供应商链上注册资料审核机制是否需要，还是任何人都可以注册？ | 产品/合规 | 未确认 |
| Q7 | 自动续订失败通知是否需要 App 推送以外的渠道（如邮件），涉及用户信息收集 | 产品/合规 | 未确认 |
| Q8 | 智能合约部署在哪条链上？是否支持多链？Gas 费由谁承担？ | 架构/产品 | 已确认：Base 网络，先单链部署，Gas 费由操作发起方承担 |
| Q9 | 智能合约代码是否开源？供应商是否可以审计合约代码？ | 产品/技术 | 已确认：合约代码开源，供应商可审计 |
| Q10 | 供应商提现是否有频率限制？是否需要设置最低提现金额？ | 产品 | 已确认：无限制，供应商可随时提现任意金额 |
| Q11 | 平台收取的 10% 手续费和 100 USDC 上线费如何分配和管理？ | 产品/财务 | 已确认：转入平台多签钱包（Gnosis Safe），至少 2/3 多签 |
| Q12 | 如果智能合约出现安全问题或需要升级，如何处理已有供应商的合约？ | 架构/产品 | 已确认：合约不可升级，发现严重漏洞需重新部署新合约 |
| Q13 | 是否需要外部安全审计？预算和时间安排？ | 产品/技术 | 已确认：采用内部审计 + 测试网验证，不进行外部审计 |
| Q14 | 是否支持退款？如何处理用户投诉？ | 产品 | 已确认：不支持退款，用户可取消订阅（当期服务继续） |
| Q15 | 供应商上线是否需要 KYC 身份验证？ | 产品/合规 | 已确认：不需要 KYC，降低准入门槛 |

---

## 十、风险提示

| 风险 | 说明 | 建议 |
|------|------|------|
| 智能合约安全风险 | 智能合约一旦部署难以修改，如存在漏洞可能导致资金损失 | 采用内部审计 + 测试网验证策略，全面测试覆盖率 >90%，Base Sepolia 验证 2+ 周 |
| 合约不可升级风险 | 合约采用不可升级设计，发现严重漏洞需重新部署 | 初期限制供应商数量（≤5），建立紧急响应流程和 Pausable 机制 |
| 链上 Gas 费波动 | 区块链网络拥堵时 Gas 费可能大幅上涨，影响用户体验和平台成本 | 已选择 Base 网络（Gas 费稳定且低廉 ~$0.30-0.50/笔），实施监控告警 |
| 供应商提现欺诈风险 | 供应商可能在收款后不提供服务或提供劣质服务 | 建立供应商信誉评分系统，用户可取消订阅，依赖声誉机制 |
| 平台资金托管合规风险 | 托管用户资金可能涉及金融监管合规问题 | 咨询法律顾问，确保智能合约托管模式符合各地区法规 |
| 供应商自建系统接入质量参差不齐 | 各供应商自有系统对 x402 协议的实现可能存在偏差，影响用户体验 | 提供官方 SDK 和测试工具，供应商接入前需通过一致性测试 |
| 用户对 Fair Use 策略不理解 | 用户可能误以为订阅制代表无限流量，超额降速后感到意外 | 订购前必须清晰展示月度流量额度，不以小字形式隐藏 |
| 钱包余额不足导致自动续订失败率高 | 用户可能不维持钱包余额 | 在到期前 7 天向余额不足用户推送余额提醒 |
| Signer Session 过期导致无法自动续订 | Session 有有效期 | 续订失败提示中明确指引用户重新授权钱包 |
| 链上支付确认延迟 | 区块链交易存在确认等待时间 | 收到支付签名后即可先激活订阅，链上确认作为事后审计 |
| RPC 服务故障 | 依赖第三方 RPC 服务可能出现故障 | 使用多 RPC 提供商 + 自动切换机制 |

---

**文档版本**：v1.4
**最后更新**：2026-03-29
**更新内容**：
- v1.3: 调整商业模式为平台托管结算,新增智能合约管理、供应商准入、提现手续费等需求
- v1.4: 更新开放问题状态(Q8-Q15 已确认),更新风险提示,反映架构师技术方案决策

**状态**：技术方案已完成,待进入开发实施阶段

**相关文档**：
- [智能合约托管结算技术方案](./智能合约托管结算技术方案.md) - 架构师已完成技术方案设计
